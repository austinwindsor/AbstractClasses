#!/usr/bin/env python

import setuptools

setuptools.setup(name='abstract_classes',
      version='1.0',
      description='Custom Classes',
      author='Austin Windsor',
      # url='https://www.python.org/sigs/distutils-sig/',
      packages=setuptools.find_packages(),
     )